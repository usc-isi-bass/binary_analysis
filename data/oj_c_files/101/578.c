#include <ctype.h>
#include <errno.h>
#include <float.h>
#include <iso646.h>
#include <limits.h>
#include <locale.h>
#include <math.h>
#include <setjmp.h>
#include <signal.h>
#include <stdarg.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

int main()
{
	int x,y,z;/*x?y?z????A?B?C?????*/
	int a,b,c;/*a?b?c????????????A?B?C?????*/
	int i;
	char rank[3];
	for(x=1;x<=2;x++)/*??x?y?z?????????*/
	for(y=0;y<=2;y++)
    for(z=0;z<=2;z++)
		if(x!=y&&x!=z&&y!=z)/*????????????if???????????*/
		{
			a=b=c=2;/*????2??????????????????????1*/
            if(x<y)
			{
				a--;
                c--;
			}
			else 
				b--;
			if(x>z)
				b--;
			if(y<z)
				c--;
			if(a!=b&&b!=c&&c!=a)
			{
				rank[a]='A';
				rank[b]='B';
				rank[c]='C';
				for(i=0;i<=2;i++)
				{
					cout<<rank[i];
				}

			}
		}
		return 0;
}
