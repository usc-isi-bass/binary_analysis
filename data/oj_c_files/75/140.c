#include <ctype.h>
#include <errno.h>
#include <float.h>
#include <iso646.h>
#include <limits.h>
#include <locale.h>
#include <math.h>
#include <setjmp.h>
#include <signal.h>
#include <stdarg.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

int main()
{
	int arrival[1000],depature[1000],i=0,j,count,max=0;
	while(true)   //????????????????????????????
	{
		cin>>arrival[i];
		i++;
		if(cin.get()!=',')
			break;
	}
	int total=i;  //?????
	i=0;
	while(true)   //????????????????
	{
		cin>>depature[i];
		i++;
		if(cin.get()!=',')
			break;
	}
	for(i=0;i<1000;i++)   //?0?1000???????????????????????
	{
		count=0;
		for(j=0;j<total;j++)
			if(i>=arrival[j]&&i<depature[j]) //?count????????????????
				count++;
		if(count>max)   //????????????
			max=count;
	}
	cout<<total<<' '<<max;
	return 0;
}

