#include <ctype.h>
#include <errno.h>
#include <float.h>
#include <iso646.h>
#include <limits.h>
#include <locale.h>
#include <math.h>
#include <setjmp.h>
#include <signal.h>
#include <stdarg.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

char str[200];
int match(int p);
char b;
char g;
int main ()
{
	// ??????match ??????????p?????????????
	// ????? ???????????????????????? ???????????
    cin.getline(str, 100);
	int len, a;
	len = strlen(str);
	b = str[0];
	g = str[len - 1];
	a = match(0);
	return 0;
}
int match(int p)
{
	int q;
	if (str[p] == b)
	{  
		q = match(p + 1);       // p?q?????? ???????? p?q????????? ????????
		cout << p << " " << q << endl;  // q?p + 1??????????????
	    return match(q + 1);
	}
	else return p;
}