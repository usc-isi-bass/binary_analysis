#include <ctype.h>
#include <errno.h>
#include <float.h>
#include <iso646.h>
#include <limits.h>
#include <locale.h>
#include <math.h>
#include <setjmp.h>
#include <signal.h>
#include <stdarg.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>



int data[10000][10000] = {0};

int main()
{
	int n;
	cin >> n;
	int x, y;
	while(1)
	{
		cin >> x >> y;
		if (x == 0 && y == 0) break;
		data[x][y] = 1; // x??y?data[x][y] = 1
	}

	//??????????????
	x = 0;
	y = 1;
	while (y < n)
	{
		if (data[x][y] == 1) // x??y???x?????????y???????
		{
			x = y; // ??x
		}
		y ++; // y??
	}

	// ????????x????????????????
	int m;
	for (m = 0; m < n; m ++)
	{
		if (m == x) continue; // ??????????

		// ?????????x??x????????x?????????
		if (! (data[m][x] == 1 && data[x][m] == 0))
		{
			break;
		}
	}
	if (m == n) // ?n????????break?????x??????
		cout << x << endl;
	else // ??????????
		cout << "NOT FOUND" << endl;
	return 0;
}